#!/bin/sh
mount -o remount,rw /
INSTDIR=/media/cryptofs/apps
DBPATH=/var/home/root/.justinput/
PATCHDIR=$INSTDIR/usr/palm/applications/com.youjf.addons/
if [ -d "${DBPATH}/db/" ]; then
    mv -f $PATCHDIR/*.db $DBPATH/db/
fi

killall -9 java

exit 0
